# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
"on":
  push:
    branches:
      - production
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    env:
      API_KEY: ${{ secrets.NEXT_PUBLIC_API_KEY }}
      APP_ID: ${{ secrets.NEXT_PUBLIC_APP_ID }}
      AUTH_DOMAIN: ${{ secrets.NEXT_PUBLIC_AUTH_DOMAIN }}
      MESSAGING_SENDER_ID: ${{ secrets.NEXT_PUBLIC_MESSAGING_SENDER_ID }}
      PROJECT_ID: ${{ secrets.NEXT_PUBLIC_PROJECT_ID }}
      STORAGE_BUCKET: ${{ secrets.NEXT_PUBLIC_STORAGE_BUCKET }}
    steps:
      - uses: actions/checkout@v2
      - run: yarn && yarn build && yarn export
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_CHAT_DEMO_9BFB2 }}"
          channelId: live
          projectId: chat-demo-9bfb2
